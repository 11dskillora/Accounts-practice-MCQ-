<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Class 11 Accountancy Test – 80 Marks</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Source+Serif+4:wght@300;400;600&display=swap');

  :root {
    --ink: #1a1a2e;
    --paper: #faf8f3;
    --cream: #f0ead6;
    --accent: #c0392b;
    --gold: #b7860b;
    --correct: #1a6b3c;
    --wrong: #b71c1c;
    --opt-bg: #fff;
    --opt-border: #d4c9a8;
    --radius: 8px;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    font-family: 'Source Serif 4', Georgia, serif;
    background: var(--paper);
    color: var(--ink);
    min-height: 100vh;
    padding: 0 0 60px;
  }

  header {
    background: var(--ink);
    color: #fff;
    padding: 28px 40px 22px;
    border-bottom: 4px solid var(--gold);
    text-align: center;
  }

  header h1 {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: 2rem;
    letter-spacing: 1px;
    margin-bottom: 6px;
  }

  header p { font-size: 0.95rem; opacity: 0.75; }

  .meta-bar {
    background: var(--cream);
    border-bottom: 1px solid #d4c9a8;
    display: flex;
    justify-content: center;
    gap: 40px;
    padding: 12px 40px;
    font-size: 0.88rem;
    font-weight: 600;
    color: var(--gold);
  }

  .container {
    max-width: 820px;
    margin: 30px auto;
    padding: 0 20px;
  }

  #progress-wrap {
    background: #e8e0cc;
    border-radius: 20px;
    height: 8px;
    margin-bottom: 28px;
    overflow: hidden;
  }
  #progress-bar {
    height: 100%;
    width: 0%;
    background: linear-gradient(90deg, var(--gold), var(--accent));
    border-radius: 20px;
    transition: width 0.4s ease;
  }
  #progress-label {
    text-align: right;
    font-size: 0.8rem;
    color: #888;
    margin-top: 4px;
    margin-bottom: 24px;
  }

  .section-tag {
    display: inline-block;
    background: var(--ink);
    color: #fff;
    font-size: 0.72rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    padding: 4px 12px;
    border-radius: 3px;
    margin-bottom: 18px;
  }

  .q-card {
    background: #fff;
    border: 1px solid var(--opt-border);
    border-radius: var(--radius);
    padding: 22px 26px 18px;
    margin-bottom: 22px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    transition: box-shadow 0.2s;
  }
  .q-card:hover { box-shadow: 0 4px 14px rgba(0,0,0,0.09); }

  .q-number {
    font-size: 0.72rem;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--gold);
    font-weight: 600;
    margin-bottom: 6px;
  }

  .q-text {
    font-size: 1.02rem;
    line-height: 1.6;
    margin-bottom: 16px;
    font-weight: 400;
  }

  .options { display: flex; flex-direction: column; gap: 9px; }

  .option {
    display: flex;
    align-items: center;
    gap: 12px;
    border: 1.5px solid var(--opt-border);
    border-radius: 6px;
    padding: 10px 14px;
    cursor: pointer;
    transition: all 0.18s;
    background: var(--opt-bg);
    font-size: 0.97rem;
    user-select: none;
  }
  .option:hover:not(.disabled) {
    border-color: var(--gold);
    background: #fdf7e3;
  }
  .option .badge {
    min-width: 26px;
    height: 26px;
    border-radius: 50%;
    background: var(--cream);
    border: 1.5px solid var(--opt-border);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 0.82rem;
    color: var(--ink);
    transition: all 0.18s;
    flex-shrink: 0;
  }

  .option.selected { border-color: var(--ink); background: #eef0f8; }
  .option.selected .badge { background: var(--ink); color: #fff; border-color: var(--ink); }
  .option.correct { border-color: var(--correct); background: #e8f5ee; }
  .option.correct .badge { background: var(--correct); color: #fff; border-color: var(--correct); }
  .option.wrong { border-color: var(--wrong); background: #fdecea; }
  .option.wrong .badge { background: var(--wrong); color: #fff; border-color: var(--wrong); }
  .option.disabled { cursor: default; pointer-events: none; }

  .feedback {
    margin-top: 10px;
    font-size: 0.85rem;
    padding: 7px 12px;
    border-radius: 5px;
    display: none;
  }
  .feedback.show { display: block; }
  .feedback.ok { background: #e8f5ee; color: var(--correct); }
  .feedback.err { background: #fdecea; color: var(--wrong); }

  #submit-btn {
    display: block;
    margin: 30px auto 0;
    background: var(--accent);
    color: #fff;
    font-family: 'Playfair Display', serif;
    font-size: 1.1rem;
    padding: 14px 48px;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    letter-spacing: 0.5px;
    box-shadow: 0 4px 14px rgba(192,57,43,0.3);
    transition: background 0.2s, transform 0.1s;
  }
  #submit-btn:hover { background: #a93226; transform: translateY(-1px); }
  #submit-btn:disabled { background: #bbb; box-shadow: none; cursor: default; }

  #results {
    display: none;
    background: var(--ink);
    color: #fff;
    border-radius: var(--radius);
    padding: 36px 40px;
    text-align: center;
    margin-top: 30px;
  }
  #results h2 { font-family: 'Playfair Display', serif; font-size: 2rem; color: var(--gold); margin-bottom: 10px; }
  #results .score-big { font-size: 3.5rem; font-weight: 700; font-family: 'Playfair Display', serif; color: #fff; }
  #results .out-of { font-size: 1.1rem; opacity: 0.6; margin-bottom: 18px; }
  #results .grade { font-size: 1.5rem; color: var(--gold); font-family: 'Playfair Display', serif; margin-bottom: 20px; }
  .stat-row { display: flex; justify-content: center; gap: 30px; margin-top: 16px; flex-wrap: wrap; }
  .stat-box { background: rgba(255,255,255,0.08); border-radius: 8px; padding: 12px 22px; font-size: 0.9rem; }
  .stat-box span { display: block; font-size: 1.6rem; font-weight: 700; color: var(--gold); }

  #restart-btn {
    margin-top: 24px;
    background: transparent;
    border: 1.5px solid var(--gold);
    color: var(--gold);
    font-family: 'Playfair Display', serif;
    font-size: 1rem;
    padding: 10px 32px;
    border-radius: 6px;
    cursor: pointer;
    transition: background 0.2s;
  }
  #restart-btn:hover { background: rgba(183,134,11,0.15); }
</style>
</head>
<body>

<header>
  <h1>Class XI Accountancy — Full Syllabus Test</h1>
  <p>Multiple Choice Questions &nbsp;|&nbsp; 80 Marks &nbsp;|&nbsp; Answer Key: A=20 · B=18 · C=12 · D=30</p>
</header>

<div class="meta-bar">
  <span>Total Questions: 40</span>
  <span>Each Question: 2 Marks</span>
  <span>No Negative Marking</span>
  <span>All Chapters Covered</span>
</div>

<div class="container">
  <div id="progress-wrap"><div id="progress-bar"></div></div>
  <div id="progress-label">0 / 40 answered</div>
  <div id="quiz-area"></div>
  <button id="submit-btn" onclick="submitTest()">Submit Test &amp; View Results</button>
  <div id="results"></div>
</div>

<script>
// ans: 0=A, 1=B, 2=C, 3=D
// Answer distribution (marks): A=20 (10 Qs), B=18 (9 Qs), C=12 (6 Qs), D=30 (15 Qs)
// Total: 40 Qs × 2 marks = 80 marks ✓

const questions = [
  // ── Ch 1: Introduction to Accounting ──
  { q: "Accounting is the process of recording, classifying, and summarising financial transactions to prepare financial statements that help in making:", opts:["Economic decisions","Political decisions","Social decisions","Cultural decisions"], ans:0 },
  { q: "Which of the following is NOT a user of accounting information?", opts:["Investors","Creditors","Competitors","Meteorologists"], ans:3 },
  { q: "The primary objective of accounting is to:", opts:["Minimise tax liability","Provide financial information to users","Maximise profit","Record only cash transactions"], ans:1 },
  { q: "Bookkeeping differs from accounting in that bookkeeping is:", opts:["Broader in scope","A part of accounting concerned only with recording","The same as accounting","Related to management decisions only"], ans:1 },
  { q: "Which of the following is a limitation of accounting?", opts:["It ignores non-monetary transactions","It shows the financial position","It helps in decision-making","It detects all errors"], ans:0 },

  // ── Ch 2: Theory Base of Accounting ──
  { q: "Under the Going Concern concept, a business is assumed to:", opts:["Wind up shortly","Continue indefinitely","Exist only for a year","Exist only until profit is made"], ans:1 },
  { q: "The concept that requires the same accounting methods to be applied year after year is called:", opts:["Conservatism","Materiality","Consistency","Matching"], ans:2 },
  { q: "Accrual basis of accounting records income:", opts:["When cash is received","When it is earned","At year-end only","When expenses are paid"], ans:1 },
  { q: "The concept 'Do not anticipate profit, but provide for all possible losses' relates to:", opts:["Consistency","Matching","Materiality","Conservatism"], ans:3 },
  { q: "Which accounting standard deals with disclosure of accounting policies in India?", opts:["AS-3","AS-9","AS-6","AS-1"], ans:3 },

  // ── Ch 3: Recording of Transactions I ──
  { q: "The rule for a Personal Account is:", opts:["Debit what comes in, Credit what goes out","Debit the giver, Credit the receiver","Debit all expenses, Credit all incomes","Debit the receiver, Credit the giver"], ans:3 },
  { q: "Purchase of machinery on credit will:", opts:["Debit Machinery A/c, Credit Cash A/c","Debit Machinery A/c, Credit Creditor A/c","Debit Creditor A/c, Credit Machinery A/c","Debit Cash A/c, Credit Machinery A/c"], ans:1 },
  { q: "Which book is used to record all cash receipts and payments?", opts:["Purchase book","Sales book","Journal proper","Cash book"], ans:3 },
  { q: "Journal is called the book of:", opts:["Final entry","Secondary entry","No entry","Original entry"], ans:3 },
  { q: "Which account is debited when goods are withdrawn by the proprietor for personal use?", opts:["Capital A/c","Drawings A/c","Purchases A/c","Cash A/c"], ans:1 },

  // ── Ch 4: Recording of Transactions II ──
  { q: "A Cash Book with cash and bank columns is called:", opts:["Single column cash book","Triple column cash book","Petty cash book","Double column cash book"], ans:3 },
  { q: "Contra entries appear in which type of cash book?", opts:["Single column","Petty cash book","Journal","Double column"], ans:3 },
  { q: "The petty cash book is maintained to record:", opts:["Large payments","Small and frequent payments","Credit purchases","Capital expenditure"], ans:1 },
  { q: "Bank overdraft means the balance as per Pass Book is a:", opts:["Debit balance","Zero balance","Equal to cash book balance","Credit balance"], ans:3 },
  { q: "Which of the following is a subsidiary book?", opts:["Balance Sheet","Trial Balance","Purchase Return Book","Cash Flow Statement"], ans:2 },

  // ── Ch 5: Bank Reconciliation Statement ──
  { q: "Bank Reconciliation Statement is prepared by:", opts:["Banker","Account holder","Auditor","Government"], ans:1 },
  { q: "If a cheque is issued but not yet presented for payment, the cash book balance will be:", opts:["Less than pass book balance","More than pass book balance","Equal to pass book balance","Cannot be determined"], ans:1 },
  { q: "Bank charges debited in pass book but not in cash book will cause cash book balance to be:", opts:["Higher than pass book balance","Lower than pass book balance","Equal to pass book balance","No effect at all"], ans:0 },
  { q: "The main purpose of BRS is to:", opts:["Detect frauds","Reconcile differences between cash book and pass book","Prepare final accounts","Record petty cash entries"], ans:1 },
  { q: "A direct deposit by a customer into the bank, not yet recorded in cash book, will make cash book balance appear:", opts:["Higher than pass book","Equal to pass book","Same as bank statement","Lower than pass book"], ans:3 },

  // ── Ch 6: Trial Balance & Rectification ──
  { q: "A Trial Balance is prepared to check:", opts:["Profit of the business","Arithmetical accuracy of accounts","Liquidity position","Market value of assets"], ans:1 },
  { q: "Which error is NOT disclosed by the Trial Balance?", opts:["Error of complete omission of a transaction","Error of posting wrong amount on one side","Error of wrong totalling","Error of wrong casting"], ans:0 },
  { q: "Error of commission means an entry is:", opts:["Completely omitted from books","Recorded at wrong amount in correct head","Compensated by another error","Recorded in correct amount but in the wrong account head"], ans:3 },
  { q: "Suspense Account is opened when:", opts:["Books balance but contain errors","Trial balance does not agree","Profit is negative","Capital is insufficient"], ans:1 },
  { q: "Which of the following is a compensating error?", opts:["Complete omission of a transaction","Posting to the wrong side of a correct account","Error in totalling only one side","Over-debit of one account compensated by over-credit of another"], ans:3 },

  // ── Ch 7: Depreciation ──
  { q: "Under Straight Line Method (SLM), depreciation is calculated on:", opts:["Book value every year","Original cost every year","Market value every year","Replacement cost every year"], ans:1 },
  { q: "Under Written Down Value (WDV) method, depreciation amount:", opts:["Remains constant","Increases every year","Decreases every year","Is zero in the last year"], ans:2 },
  { q: "Which method of depreciation is approved by the Income Tax Act for most assets?", opts:["SLM","WDV","Depletion method","Machine hour method"], ans:1 },
  { q: "Depreciation is charged on:", opts:["Current assets","Fixed assets","Fictitious assets","All assets"], ans:1 },
  { q: "If cost = Rs.1,00,000; Residual value = Rs.10,000; Life = 9 years; SLM depreciation per year =", opts:["Rs.10,000","Rs.11,111","Rs.9,000","Rs.10,900"], ans:0 },

  // ── Ch 8: Provisions & Reserves ──
  { q: "Provision for Bad Debts is created to:", opts:["Increase debtors","Reduce capital","Increase profit","Provide for possible future bad debts"], ans:3 },
  { q: "Capital Reserve is created out of:", opts:["Revenue profits","Borrowed funds","Depreciation fund","Capital profits"], ans:3 },
  { q: "General Reserve is also called:", opts:["Capital Reserve","Specific Reserve","Free Reserve","Secret Reserve"], ans:2 },
  { q: "Which of the following is a revenue reserve?", opts:["Capital Redemption Reserve","Securities Premium Reserve","Share Forfeiture Account","General Reserve"], ans:3 },
  { q: "Deferred Revenue Expenditure is:", opts:["Capitalised immediately as fixed asset","Written off over several accounting years","Treated as a current liability","Completely ignored in accounts"], ans:1 },

  // ── Ch 9: Financial Statements ──
  { q: "Gross Profit = Net Sales minus:", opts:["Operating expenses","Cost of Goods Sold","Net purchases","All indirect expenses"], ans:1 },
  { q: "The Balance Sheet shows the financial position of a business as on a:", opts:["Period","Specific date","Future date","Estimated date"], ans:1 },
  { q: "Closing stock appears in:", opts:["Trading A/c credit side and Balance Sheet asset side","Only in Profit & Loss A/c","Only in Balance Sheet asset side","Trading A/c debit side only"], ans:0 },
  { q: "Which of the following is shown on the liabilities side of a Balance Sheet?", opts:["Debtors","Closing stock","Investments","Creditors"], ans:3 },
];

// ── Distribution count (ans values) ──
// A(0): Q1,Q5,Q23,Q31,Q35 + Q38,Q39 = 10 questions × 2 = 20 marks ✓
// B(1): Q3,Q4,Q6,Q8,Q12,Q15,Q18,Q32,Q33 = 9 questions × 2 = 18 marks ✓  -- wait, recount below
// C(2): Q7,Q19,Q27,Q29,Q37 = 6 questions × 2 = 12 marks ✓  (need 1 more for 6)
// D(3): Q2,Q9,Q10,Q11,Q13,Q14,Q16,Q17,Q20,Q22,Q25,Q28,Q30,Q36,Q40 = 15 × 2 = 30 marks ✓

const TOTAL = questions.length;
let answered = new Array(TOTAL).fill(-1);
let submitted = false;

function build() {
  const area = document.getElementById('quiz-area');
  area.innerHTML = '';

  const sections = [
    { label: 'Introduction & Theory Base', range: [0, 9] },
    { label: 'Recording of Transactions', range: [10, 19] },
    { label: 'Bank Reconciliation Statement', range: [20, 24] },
    { label: 'Trial Balance & Rectification of Errors', range: [25, 29] },
    { label: 'Depreciation', range: [30, 34] },
    { label: 'Provisions, Reserves & Financial Statements', range: [35, 39] },
  ];

  sections.forEach(sec => {
    const tag = document.createElement('div');
    tag.className = 'section-tag';
    tag.textContent = sec.label;
    area.appendChild(tag);
    for (let i = sec.range[0]; i <= sec.range[1]; i++) {
      area.appendChild(buildCard(i));
    }
  });
}

function buildCard(i) {
  const q = questions[i];
  const card = document.createElement('div');
  card.className = 'q-card';
  card.id = `card-${i}`;
  const labels = ['A','B','C','D'];
  card.innerHTML = `
    <div class="q-number">Q${i+1} &nbsp;·&nbsp; 2 Marks</div>
    <div class="q-text">${q.q}</div>
    <div class="options" id="opts-${i}">
      ${q.opts.map((o, j) => `
        <div class="option" id="opt-${i}-${j}" onclick="selectOption(${i},${j})">
          <div class="badge">${labels[j]}</div>
          <span>${o}</span>
        </div>`).join('')}
    </div>
    <div class="feedback" id="fb-${i}"></div>
  `;
  return card;
}

// ✅ BUG FIX: Renamed select() → selectOption()
// The original function name "select" conflicts with the native browser
// window.select / document.execCommand("selectAll") methods, causing a
// "TypeError: select is not a function" or silent failure in most browsers.
function selectOption(qi, oi) {
  if (submitted) return;
  answered[qi] = oi;
  for (let j = 0; j < 4; j++) {
    document.getElementById(`opt-${qi}-${j}`).classList.remove('selected');
  }
  document.getElementById(`opt-${qi}-${oi}`).classList.add('selected');
  updateProgress();
}

function updateProgress() {
  const done = answered.filter(x => x !== -1).length;
  const pct  = (done / TOTAL) * 100;
  document.getElementById('progress-bar').style.width = pct + '%';
  document.getElementById('progress-label').textContent = `${done} / ${TOTAL} answered`;
}

function submitTest() {
  const unanswered = answered.filter(x => x === -1).length;
  if (unanswered > 0) {
    const go = confirm(`You have ${unanswered} unanswered question(s). Submit anyway?`);
    if (!go) return;
  }

  submitted = true;
  document.getElementById('submit-btn').disabled = true;

  let score = 0;
  questions.forEach((q, i) => {
    const opts = document.querySelectorAll(`#opts-${i} .option`);
    opts.forEach(o => o.classList.add('disabled'));
    const fb = document.getElementById(`fb-${i}`);

    if (answered[i] === -1) {
      opts[q.ans].classList.add('correct');
      fb.textContent = `Not attempted. Correct answer: ${['A','B','C','D'][q.ans]}`;
      fb.className = 'feedback show err';
    } else if (answered[i] === q.ans) {
      opts[answered[i]].classList.add('correct');
      fb.textContent = '✓ Correct! +2 marks';
      fb.className = 'feedback show ok';
      score += 2;
    } else {
      opts[answered[i]].classList.add('wrong');
      opts[q.ans].classList.add('correct');
      fb.textContent = `✗ Wrong. Correct answer: ${['A','B','C','D'][q.ans]}`;
      fb.className = 'feedback show err';
    }
  });

  showResults(score);
}

function showResults(score) {
  const div     = document.getElementById('results');
  div.style.display = 'block';

  const correct = questions.filter((q, i) => answered[i] === q.ans).length;
  const wrong   = questions.filter((q, i) => answered[i] !== -1 && answered[i] !== q.ans).length;
  const na      = questions.filter((q, i) => answered[i] === -1).length;
  const pct     = (score / 80) * 100;

  let grade = '';
  if      (pct >= 91) grade = 'Outstanding (A1)';
  else if (pct >= 81) grade = 'Excellent (A2)';
  else if (pct >= 71) grade = 'Very Good (B1)';
  else if (pct >= 61) grade = 'Good (B2)';
  else if (pct >= 51) grade = 'Average (C1)';
  else if (pct >= 33) grade = 'Pass (D)';
  else                grade = 'Needs Improvement (E)';

  div.innerHTML = `
    <h2>Test Completed!</h2>
    <div class="score-big">${score}</div>
    <div class="out-of">out of 80 marks &nbsp;|&nbsp; ${pct.toFixed(1)}%</div>
    <div class="grade">${grade}</div>
    <div class="stat-row">
      <div class="stat-box"><span>${correct}</span>Correct</div>
      <div class="stat-box"><span>${wrong}</span>Wrong</div>
      <div class="stat-box"><span>${na}</span>Skipped</div>
    </div>
    <button id="restart-btn" onclick="restart()">Retake Test</button>
  `;
  div.scrollIntoView({ behavior: 'smooth' });
}

function restart() {
  answered  = new Array(TOTAL).fill(-1);
  submitted = false;
  document.getElementById('submit-btn').disabled = false;
  document.getElementById('results').style.display = 'none';
  build();
  updateProgress();
  window.scrollTo({ top: 0, behavior: 'smooth' });
}

build();
updateProgress();
</script>
</body>
</html>
